<Window x:Class="Template_1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Template_1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="450">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- Левая колонка -->
        <Button Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2"  Content="Кнопка 1" />
        <Button Grid.Column="0" Grid.Row="1" Content="Кнопка 3" />
        <Button Grid.Column="0" Grid.Row="2" Content="Кнопка 4" />

        <!-- Правая верхняя -->
        <Button Grid.Column="2" Grid.Row="0" Content="Кнопка 2" />

        <!-- -->
        <Button Grid.Column="1" Grid.Row="1" Grid.RowSpan="2" Grid.ColumnSpan="2" Content="Кнопка 4" />

    </Grid>
</Window>
