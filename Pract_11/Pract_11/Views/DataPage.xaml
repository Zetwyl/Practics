<Page x:Class="Pract_11.Views.DataPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Pract_11.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Loaded="Page_Loaded"
      Title="DataPage">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <Button Height="20"
                Margin="10,20,0,20"
                Click="Button_Click"
                Content="Назад"
                HorizontalAlignment="Left"
                Width="70"/>

        <TextBox Height="20"
                 x:Name="poisk"
                 Margin="90,20,0,20"
                 HorizontalAlignment="Left"
                 Width="100"/>

        <Button Height="20"
                x:Name="poisk_btn"
                Margin="200,20,0,20"
                Click="Button_Click_4"
                Content="Поиск"
                HorizontalAlignment="Left"
                Width="70"/>

        <TextBox Height="20"
                 x:Name="filter"
                 Margin="280,20,0,20"
                 HorizontalAlignment="Left"
                 Width="120"/>

        <Button Height="20"
                x:Name="filter_btn"
                Margin="410,20,0,20"
                Click="filter_btn_Click"
                Content="Фильтрация"
                HorizontalAlignment="Left"
                Width="70"/>
        
        <Button Height="20"
                x:Name="sortirovka_btn"
                Margin="500,20,0,20"
                Click="sortirovka_btn_Click"
                Content="Сортировка"
                HorizontalAlignment="Left"
                Width="70"/>

        <DataGrid Grid.Row="1"
                  AutoGenerateColumns="False"
                  x:Name="UsersGrid">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Width="*" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Логин" Width="*" Binding="{Binding Login}"/>
                <DataGridTextColumn Header="Пароль" Width="*" Binding="{Binding Password}"/>
                <DataGridTextColumn Header="Роль" Width="*" Binding="{Binding Role.Title}"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2"
                    HorizontalAlignment="Center"
                    Orientation="Horizontal"
                    VerticalAlignment="Center">
            <Button Height="24"
                    Click="Button_Click_1"
                    Content="Добавить"
                    Width="150"/>
            <Button Height="24"
                    Click="Button_Click_2"
                    Content="Удалить"
                    Width="150"/>
        </StackPanel>
    </Grid>
</Page>
